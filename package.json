{
  "name": "attnlab",
  "version": "1.0.0",
  "description": "An interactive multi-head attention playground: real attention math, hand-rolled matrix code, zero runtime dependencies.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc && node build.mjs",
    "demo": "node demo.mjs",
    "test": "tsc && node --test",
    "report": "node report.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0"
  }
}
